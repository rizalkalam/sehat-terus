---
phase: 01-environment-database-bedrock
plan: "02"
type: execute
<<<<<<< HEAD
wave: 1
depends_on: []
files_modified:
  - backend/package.json
  - backend/tsconfig.json
  - backend/Dockerfile
  - backend/.dockerignore
  - backend/src/index.ts
  - backend/src/app.ts
autonomous: true
requirements:
  - API-01
=======
wave: 2
depends_on:
  - "01-01"
files_modified:
  - prisma/schema.prisma
  - src/lib/db.ts
  - scripts/test-db-connection.ts
  - package.json
  - Makefile
autonomous: true
requirements:
  - API-02
>>>>>>> 5e06b1996e0743755c7783916dbef93a956a0aa8
user_setup: []

must_haves:
  truths:
<<<<<<< HEAD
    - "Running compilation 'npx tsc --noEmit' in the backend directory completes successfully."
    - "backend/tsconfig.json is configured with strict: true."
  artifacts:
    - "backend/package.json"
    - "backend/tsconfig.json"
    - "backend/Dockerfile"
    - "backend/src/index.ts"
    - "backend/src/app.ts"
  key_links:
    - "backend/src/index.ts imports and starts Express app."
---

<objective>
Scaffold the Express.js API backend configured with TypeScript, CORS support, basic healthcheck endpoint, and configure its development/production Dockerfile.

Purpose: To establish the server-side API application foundation which will expose data analysis and aggregation endpoints to the frontend.
Output: Initialized Express.js backend, index and app entrypoints, and Docker configuration.
=======
    - "Running 'npx prisma validate' runs without errors."
    - "TypeScript compilation of scripts/test-db-connection.ts completes without type errors."
  artifacts:
    - "prisma/schema.prisma"
    - "src/lib/db.ts"
    - "scripts/test-db-connection.ts"
  key_links:
    - "src/lib/db.ts imports and uses @prisma/client to connect."
---

<objective>
Configure Prisma ORM with the PostgreSQL driver, define the RekamMedis schema incorporating indexes on tanggal_kunjungan and kecamatan_domisili, run migrations, and implement the database connection check script.

Purpose: To ensure the application has a type-safe interface and optimized schema for querying large clinical datasets.
Output: Prisma configuration, RekamMedis schema with B-Tree indexes, database connection singleton, and a validation script.
>>>>>>> 5e06b1996e0743755c7783916dbef93a956a0aa8
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-environment-database-bedrock/01-CONTEXT.md
@.planning/phases/01-environment-database-bedrock/01-RESEARCH.md
<<<<<<< HEAD
=======
@.planning/phases/01-environment-database-bedrock/01-01-PLAN.md
>>>>>>> 5e06b1996e0743755c7783916dbef93a956a0aa8
</context>

<tasks>

<task type="auto">
<<<<<<< HEAD
  <name>Task 1: Scaffold Express.js with TypeScript and middleware</name>
  <files>backend/package.json, backend/tsconfig.json, backend/src/app.ts, backend/src/index.ts</files>
  <read_first>.planning/phases/01-environment-database-bedrock/01-RESEARCH.md</read_first>
  <action>
    Create a 'backend/' directory.
    Initialize backend/package.json with express, cors, dotenv, and devDependencies: typescript, @types/express, @types/node, @types/cors, tsx.
    Create backend/tsconfig.json with "strict": true and configure target, module resolution, and output directory.
    Create backend/src/app.ts: instantiate Express app, configure cors and express.json() middlewares, and add a simple GET /health healthcheck endpoint returning OK.
    Create backend/src/index.ts to start the server listening on port 5000.
  </action>
  <verify>cd backend && npx tsc --noEmit</verify>
  <acceptance_criteria>
    - backend/tsconfig.json has "strict": true
    - backend/src/app.ts contains GET /health endpoint
    - npx tsc --noEmit executes without compilation errors
  </acceptance_criteria>
  <done>Express.js server scaffolding is written and verified for TS type safety.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Backend Dockerfile</name>
  <files>backend/Dockerfile, backend/.dockerignore</files>
  <read_first>backend/package.json</read_first>
  <action>
    Create a Dockerfile in the backend/ directory using a Node.js base image. Configure it to support building TypeScript, installing dependencies, exposing port 5000, and starting the server using node or tsx.
    Create a .dockerignore file in the backend/ directory to ignore node_modules and build outputs.
  </action>
  <verify>test -f backend/Dockerfile</verify>
  <acceptance_criteria>
    - backend/Dockerfile exposes port 5000
    - backend/.dockerignore ignores node_modules
  </acceptance_criteria>
  <done>Docker configuration for the Express.js API backend is complete.</done>
=======
  <name>Task 1: Configure Prisma ORM and define RekamMedis model with B-Tree indexes</name>
  <files>prisma/schema.prisma, package.json</files>
  <read_first>package.json, .planning/phases/01-environment-database-bedrock/01-RESEARCH.md</read_first>
  <action>
    Initialize Prisma setup in the project.
    Create a schema.prisma file in prisma/ directory.
    Configure the datasource provider as "postgresql" and generator client as "prisma-client-js".
    Define the RekamMedis model:
      - id (String, UUID, @id, @default(uuid()))
      - tanggal_kunjungan (DateTime)
      - kode_icd10 (String)
      - nama_penyakit (String)
      - kecamatan_domisili (String)
    Add B-Tree indexes for tanggal_kunjungan and kecamatan_domisili using @@index([tanggal_kunjungan]) and @@index([kecamatan_domisili]) at the model level.
  </action>
  <verify>npx prisma validate</verify>
  <acceptance_criteria>
    - prisma/schema.prisma model RekamMedis contains @@index([tanggal_kunjungan])
    - prisma/schema.prisma model RekamMedis contains @@index([kecamatan_domisili])
    - npx prisma validate exits cleanly with code 0
  </acceptance_criteria>
  <done>Prisma model defined and validated using prisma parser checks.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Prisma Singleton client and test-db-connection script</name>
  <files>src/lib/db.ts, scripts/test-db-connection.ts, package.json, Makefile</files>
  <read_first>prisma/schema.prisma</read_first>
  <action>
    Create src/lib/db.ts to instantiate the PrismaClient as a singleton, preventing duplicate instances during Next.js live reloads.
    Create scripts/test-db-connection.ts which imports the Prisma singleton client, runs a test connection query (e.g. counting RekamMedis rows or selecting 1), handles errors gracefully, and prints status.
    Add helper commands to package.json (e.g. "db:migrate", "db:push", "test:db") and a Makefile with basic targets for setup and docker controls.
  </action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - src/lib/db.ts exports a singleton instance of PrismaClient
    - scripts/test-db-connection.ts compiles without TypeScript compile errors
    - Makefile contains db setup and migration commands
  </acceptance_criteria>
  <done>Singleton DB client and test scripts are fully written and verified for compilation.</done>
>>>>>>> 5e06b1996e0743755c7783916dbef93a956a0aa8
</task>

</tasks>

<verification>
Before declaring plan complete:
<<<<<<< HEAD
- [ ] Run `cd backend && npx tsc --noEmit` to verify type checking.
</verification>

<success_criteria>
- Express.js compilation passes without error.
- Dockerfile is present and valid.
=======
- [ ] Run `npx prisma validate` to confirm syntax is correct.
- [ ] Run typescript typechecking using `npx tsc --noEmit`.
</verification>

<success_criteria>
- All tasks completed successfully.
- prisma/schema.prisma contains RekamMedis with UUID id and B-Tree indexes on indexed fields.
- src/lib/db.ts contains the singleton client check.
>>>>>>> 5e06b1996e0743755c7783916dbef93a956a0aa8
</success_criteria>

<output>
After completion, create `.planning/phases/01-environment-database-bedrock/01-02-SUMMARY.md`
</output>
