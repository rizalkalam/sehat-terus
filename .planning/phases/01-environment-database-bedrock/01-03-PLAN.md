---
phase: 01-environment-database-bedrock
plan: "03"
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - docker-compose.yml
  - .env.example
  - .env
  - backend/src/config/database.ts
  - backend/src/models/RekamMedis.ts
  - backend/src/models/index.ts
  - scripts/test-db-connection.ts
  - package.json
  - Makefile
autonomous: true
requirements:
  - API-01
  - API-02
user_setup: []

must_haves:
  truths:
    - "Running 'docker compose config' returns valid yaml schema without error."
    - "Running 'npx tsx scripts/test-db-connection.ts' connects successfully to the database and verifies database setup."
  artifacts:
    - "docker-compose.yml"
    - ".env.example"
    - ".env"
    - "backend/src/config/database.ts"
    - "backend/src/models/RekamMedis.ts"
    - "scripts/test-db-connection.ts"
  key_links:
    - "backend/src/models/RekamMedis.ts imports and utilizes the sequelize instance from backend/src/config/database.ts."
---

<objective>
Configure the database environment using Sequelize ORM in the backend, implement the RekamMedis model containing B-Tree indexes, set up the root docker-compose.yml to run PostgreSQL along with the backend/frontend containers, and create a connection testing script.

Purpose: To ensure the monorepo has a functioning containerized database backend ready to serve health data.
Output: Root docker compose file, backend Sequelize configuration, RekamMedis database models, and connection check scripts.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-environment-database-bedrock/01-CONTEXT.md
@.planning/phases/01-environment-database-bedrock/01-RESEARCH.md
@.planning/phases/01-environment-database-bedrock/01-01-PLAN.md
@.planning/phases/01-environment-database-bedrock/01-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure Sequelize and define RekamMedis model with B-Tree indexes</name>
  <files>backend/src/config/database.ts, backend/src/models/RekamMedis.ts, backend/src/models/index.ts, backend/package.json</files>
  <read_first>backend/package.json, .planning/phases/01-environment-database-bedrock/01-RESEARCH.md</read_first>
  <action>
    Install pg, pg-hstore, and sequelize inside backend/ (if not already installed).
    Create backend/src/config/database.ts containing the Sequelize instance creation with pooling and configuration derived from DATABASE_URL.
    Create backend/src/models/RekamMedis.ts using standard Sequelize class syntax: define fields id (UUID, default uuid), tanggal_kunjungan (Date), kode_icd10 (String), nama_penyakit (String), kecamatan_domisili (String).
    Add B-Tree indexes configuration to the model options object for tanggal_kunjungan and kecamatan_domisili.
    Create backend/src/models/index.ts to export the models and sequelize instance.
  </action>
  <verify>cd backend && npx tsc --noEmit</verify>
  <acceptance_criteria>
    - backend/src/models/RekamMedis.ts contains model indexes definition using BTREE method
    - backend/src/config/database.ts exports Sequelize instance
    - Typescript type check passes without errors
  </acceptance_criteria>
  <done>Sequelize models and database connections are coded and typechecked.</done>
</task>

<task type="auto">
  <name>Task 2: Configure root docker-compose.yml and database test script</name>
  <files>docker-compose.yml, .env.example, .env, scripts/test-db-connection.ts, package.json, Makefile</files>
  <read_first>docker-compose.yml</read_first>
  <action>
    Create a root docker-compose.yml configuring three services:
      - db: PostgreSQL 16-alpine image, ports 5432:5432 exposed to host, healthcheck, and postgres_data volume.
      - backend: builds from backend/, maps port 5000:5000, loads environment variables, depends on db healthy.
      - frontend: builds from frontend/, maps port 3000:3000, depends on backend.
    Create a root .env.example and .env containing the DATABASE_URL (pointing to localhost:5432 for local CLI tools execution).
    Create a root scripts/test-db-connection.ts importing the database config and testing database synchronization/connection.
    Create package.json and Makefile at the root with helper commands for starting containers, running migrations, and running tests.
  </action>
  <verify>docker compose config</verify>
  <acceptance_criteria>
    - docker-compose.yml contains db, backend, and frontend services
    - docker-compose.yml db service exposes port 5432 to the host (per D-02)
    - scripts/test-db-connection.ts compiles and executes cleanly
  </acceptance_criteria>
  <done>Docker orchestration and database validation scripting is finished and verified.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Run `docker compose config` to verify the configuration syntax is correct.
- [ ] Run `npx tsx scripts/test-db-connection.ts` and ensure connection is verified.
</verification>

<success_criteria>
- docker-compose.yml is valid.
- test-db-connection.ts connection verification completes successfully.
</success_criteria>

<output>
After completion, create `.planning/phases/01-environment-database-bedrock/01-03-SUMMARY.md`
</output>
