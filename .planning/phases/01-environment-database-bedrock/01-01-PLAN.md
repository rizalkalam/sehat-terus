---
phase: 01-environment-database-bedrock
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
<<<<<<< HEAD
  - frontend/package.json
  - frontend/tsconfig.json
  - frontend/Dockerfile
  - frontend/.dockerignore
  - frontend/src/app/layout.tsx
  - frontend/src/app/page.tsx
  - frontend/src/components/Sidebar.tsx
  - frontend/src/components/Navbar.tsx
  - frontend/src/lib/utils.ts
=======
  - package.json
  - tsconfig.json
  - Dockerfile
  - docker-compose.yml
  - .dockerignore
  - .env.example
  - .env
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/globals.css
>>>>>>> 5e06b1996e0743755c7783916dbef93a956a0aa8
autonomous: true
requirements:
  - API-01
user_setup: []

must_haves:
  truths:
<<<<<<< HEAD
    - "Running 'npm run build' in the frontend directory completes successfully."
    - "frontend/tsconfig.json is configured with strict: true."
  artifacts:
    - "frontend/package.json"
    - "frontend/tsconfig.json"
    - "frontend/Dockerfile"
    - "frontend/src/app/layout.tsx"
    - "frontend/src/components/Sidebar.tsx"
    - "frontend/src/components/Navbar.tsx"
  key_links:
    - "frontend/src/app/layout.tsx renders Navbar and Sidebar components."
---

<objective>
Scaffold the Next.js App Router frontend configured with TypeScript, Tailwind CSS, Navbar/Sidebar layouts, and configure its development/production Dockerfile.

Purpose: To establish a responsive layout skeleton and branding for the public-facing surveillance application.
Output: Initialized Next.js frontend, layout shell, global CSS styles, and Docker configuration.
=======
    - "Running 'docker compose config' returns valid yaml schema without error."
    - "Next.js configuration supports React 19 and runs cleanly in development environment."
  artifacts:
    - "package.json"
    - "tsconfig.json"
    - "Dockerfile"
    - "docker-compose.yml"
    - ".dockerignore"
    - "src/app/layout.tsx"
    - "src/app/page.tsx"
  key_links:
    - "web service in docker-compose.yml links to db service."
---

<objective>
Scaffold a Next.js App Router project configured with TypeScript and set up the multi-stage Dockerfile and docker-compose.yml configuration to run both Next.js and PostgreSQL services on a shared Docker network.

Purpose: To establish a stable, containerized environment that matches the defined technology stack and serves as the baseline for all subsequent phases.
Output: Initialized Next.js project codebase, docker configuration files, and network configuration.
>>>>>>> 5e06b1996e0743755c7783916dbef93a956a0aa8
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-environment-database-bedrock/01-CONTEXT.md
@.planning/phases/01-environment-database-bedrock/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
<<<<<<< HEAD
  <name>Task 1: Scaffold Next.js App Router with TypeScript and Tailwind CSS</name>
  <files>frontend/package.json, frontend/tsconfig.json, frontend/src/app/layout.tsx, frontend/src/app/page.tsx</files>
  <read_first>.planning/phases/01-environment-database-bedrock/01-RESEARCH.md</read_first>
  <action>
    Create a 'frontend/' directory.
    Scaffold the Next.js app in 'frontend/' using create-next-app non-interactively or manually create standard files (Next.js 15.2.9, React 19).
    Configure frontend/tsconfig.json to enforce strict type checking: set "strict": true.
    Install lucide-react, tailwind-merge, and clsx inside 'frontend/'.
  </action>
  <verify>cd frontend && npx next --version</verify>
  <acceptance_criteria>
    - frontend/tsconfig.json has "strict": true
    - frontend/package.json has "next": "15.2.9" or equivalent
  </acceptance_criteria>
  <done>Next.js app is scaffolded with TypeScript strict mode enabled in frontend/.</done>
</task>

<task type="auto">
  <name>Task 2: Build layout shell and configure Dockerfile</name>
  <files>frontend/src/components/Navbar.tsx, frontend/src/components/Sidebar.tsx, frontend/src/lib/utils.ts, frontend/src/app/layout.tsx, frontend/src/app/page.tsx, frontend/Dockerfile, frontend/.dockerignore</files>
  <read_first>frontend/package.json</read_first>
  <action>
    Create frontend/src/components/Navbar.tsx (branding and indicator statuses) and frontend/src/components/Sidebar.tsx (routes for geospatial map, trends, early warning).
    Add frontend/src/lib/utils.ts with a cn class merging utility.
    Update layout.tsx to wrap pages inside the Navbar/Sidebar layout container.
    Scaffold page.tsx with grid placeholders for Map and Region Details.
    Create a multi-stage Dockerfile for Next.js (utilizing output: 'standalone') and a .dockerignore file in the frontend/ directory.
  </action>
  <verify>cd frontend && npm run build</verify>
  <acceptance_criteria>
    - Sidebar component contains navigation links matching pages
    - frontend/Dockerfile exposes port 3000
    - Next.js build finishes successfully in frontend/
  </acceptance_criteria>
  <done>Frontend UI shell is completed and Dockerfile is configured.</done>
=======
  <name>Task 1: Scaffold Next.js App Router and configure TypeScript</name>
  <files>package.json, tsconfig.json, src/app/layout.tsx, src/app/page.tsx, src/app/globals.css</files>
  <read_first>.planning/phases/01-environment-database-bedrock/01-CONTEXT.md, .planning/phases/01-environment-database-bedrock/01-RESEARCH.md</read_first>
  <action>
    Scaffold the Next.js project inside the root workspace using standard Next.js files (or create standard skeleton structure directly) using Next.js 15.2.13 and TypeScript 5.7.3.
    Configure tsconfig.json to enforce strict type checking: set "strict": true.
    Setup globals.css with basic Tailwind v4 rules and core Next.js components layout.tsx and page.tsx with placeholder code.
    Define basic package.json scripts for running in development and production modes.
  </action>
  <verify>npx next --version</verify>
  <acceptance_criteria>
    - tsconfig.json has "strict": true
    - package.json dependencies contain "next": "^15.2.13" or equivalent and "typescript": "^5.7.3" or equivalent
    - next build completes successfully locally
  </acceptance_criteria>
  <done>Next.js project is scaffolded and TypeScript strict checks pass during compile check.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Dockerfile and docker-compose.yml</name>
  <files>Dockerfile, docker-compose.yml, .dockerignore, .env.example, .env</files>
  <read_first>package.json</read_first>
  <action>
    Create a multi-stage Dockerfile for Next.js to enable build optimization.
    Create a docker-compose.yml configuration with:
      - Service db: PostgreSQL image postgres:16-alpine, mapped port 5432:5432, persistent volume pgdata, and proper healthcheck.
      - Service web: builds Next.js application, loads environment variables, exposes port 3000:3000, connects to db service, and utilizes depends_on with healthcheck condition.
    Create .dockerignore and .env.example files.
    Create a .env file locally with standard dummy connection string for database tooling.
  </action>
  <verify>docker compose config</verify>
  <acceptance_criteria>
    - docker-compose.yml has db port 5432 exposed to the host (per D-02)
    - docker-compose.yml web service depends_on has condition: service_healthy for the db service
    - .dockerignore ignores node_modules and .next directories
  </acceptance_criteria>
  <done>Docker and Docker Compose settings are written and verified using config verification commands.</done>
>>>>>>> 5e06b1996e0743755c7783916dbef93a956a0aa8
</task>

</tasks>

<verification>
Before declaring plan complete:
<<<<<<< HEAD
- [ ] Run `cd frontend && npx tsc --noEmit` to verify type checking.
- [ ] Run `cd frontend && npm run lint` and ensure no ESLint errors exist.
</verification>

<success_criteria>
- Next.js build passes.
- Layout renders Navbar, Sidebar, and content page layout correctly.
- tsconfig.json has strict parameter as true.
=======
- [ ] Run `docker compose config` to verify the configuration syntax is correct.
- [ ] Run typescript typechecking using `npx tsc --noEmit` and check that it passes.
- [ ] Run `npm run lint` and ensure no ESLint errors occur.
</verification>

<success_criteria>
- All tasks completed successfully.
- docker-compose.yml file contains both web and db services properly configured.
- tsconfig.json strict parameter is true.
>>>>>>> 5e06b1996e0743755c7783916dbef93a956a0aa8
</success_criteria>

<output>
After completion, create `.planning/phases/01-environment-database-bedrock/01-01-SUMMARY.md`
</output>
