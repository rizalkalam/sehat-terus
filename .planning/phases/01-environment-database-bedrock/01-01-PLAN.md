---
phase: 01-environment-database-bedrock
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/tsconfig.json
  - frontend/Dockerfile
  - frontend/.dockerignore
  - frontend/src/app/layout.tsx
  - frontend/src/app/page.tsx
  - frontend/src/components/Sidebar.tsx
  - frontend/src/components/Navbar.tsx
  - frontend/src/lib/utils.ts
autonomous: true
requirements:
  - API-01
user_setup: []

must_haves:
  truths:
    - "Running 'npm run build' in the frontend directory completes successfully."
    - "frontend/tsconfig.json is configured with strict: true."
  artifacts:
    - "frontend/package.json"
    - "frontend/tsconfig.json"
    - "frontend/Dockerfile"
    - "frontend/src/app/layout.tsx"
    - "frontend/src/components/Sidebar.tsx"
    - "frontend/src/components/Navbar.tsx"
  key_links:
    - "frontend/src/app/layout.tsx renders Navbar and Sidebar components."
---

<objective>
Scaffold the Next.js App Router frontend configured with TypeScript, Tailwind CSS, Navbar/Sidebar layouts, and configure its development/production Dockerfile.

Purpose: To establish a responsive layout skeleton and branding for the public-facing surveillance application.
Output: Initialized Next.js frontend, layout shell, global CSS styles, and Docker configuration.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-environment-database-bedrock/01-CONTEXT.md
@.planning/phases/01-environment-database-bedrock/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js App Router with TypeScript and Tailwind CSS</name>
  <files>frontend/package.json, frontend/tsconfig.json, frontend/src/app/layout.tsx, frontend/src/app/page.tsx</files>
  <read_first>.planning/phases/01-environment-database-bedrock/01-RESEARCH.md</read_first>
  <action>
    Create a 'frontend/' directory.
    Scaffold the Next.js app in 'frontend/' using create-next-app non-interactively or manually create standard files (Next.js 15.2.9, React 19).
    Configure frontend/tsconfig.json to enforce strict type checking: set "strict": true.
    Install lucide-react, tailwind-merge, and clsx inside 'frontend/'.
  </action>
  <verify>cd frontend && npx next --version</verify>
  <acceptance_criteria>
    - frontend/tsconfig.json has "strict": true
    - frontend/package.json has "next": "15.2.9" or equivalent
  </acceptance_criteria>
  <done>Next.js app is scaffolded with TypeScript strict mode enabled in frontend/.</done>
</task>

<task type="auto">
  <name>Task 2: Build layout shell and configure Dockerfile</name>
  <files>frontend/src/components/Navbar.tsx, frontend/src/components/Sidebar.tsx, frontend/src/lib/utils.ts, frontend/src/app/layout.tsx, frontend/src/app/page.tsx, frontend/Dockerfile, frontend/.dockerignore</files>
  <read_first>frontend/package.json</read_first>
  <action>
    Create frontend/src/components/Navbar.tsx (branding and indicator statuses) and frontend/src/components/Sidebar.tsx (routes for geospatial map, trends, early warning).
    Add frontend/src/lib/utils.ts with a cn class merging utility.
    Update layout.tsx to wrap pages inside the Navbar/Sidebar layout container.
    Scaffold page.tsx with grid placeholders for Map and Region Details.
    Create a multi-stage Dockerfile for Next.js (utilizing output: 'standalone') and a .dockerignore file in the frontend/ directory.
  </action>
  <verify>cd frontend && npm run build</verify>
  <acceptance_criteria>
    - Sidebar component contains navigation links matching pages
    - frontend/Dockerfile exposes port 3000
    - Next.js build finishes successfully in frontend/
  </acceptance_criteria>
  <done>Frontend UI shell is completed and Dockerfile is configured.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Run `cd frontend && npx tsc --noEmit` to verify type checking.
- [ ] Run `cd frontend && npm run lint` and ensure no ESLint errors exist.
</verification>

<success_criteria>
- Next.js build passes.
- Layout renders Navbar, Sidebar, and content page layout correctly.
- tsconfig.json has strict parameter as true.
</success_criteria>

<output>
After completion, create `.planning/phases/01-environment-database-bedrock/01-01-SUMMARY.md`
</output>
