---
phase: 02-mock-ingestion-geographic-mapping-validation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/public/geojson/sleman-kecamatan.geojson
  - scripts/validate-geojson.ts
autonomous: true
requirements:
  - SEED-02
user_setup: []

must_haves:
  truths:
    - "frontend/public/geojson/sleman-kecamatan.geojson is valid JSON."
    - "The GeoJSON file contains a FeatureCollection where each feature represents a kecamatan in Sleman."
    - "Every feature has properties including 'name' (string) corresponding to the Sleman sub-districts."
  artifacts:
    - "frontend/public/geojson/sleman-kecamatan.geojson"
    - "scripts/validate-geojson.ts"
---

<objective>
Integrate and validate a simplified Sleman Regency (Daerah Istimewa Yogyakarta) kecamatan GeoJSON boundary file, ensuring all sub-district names are correctly represented.

Purpose: To provide the geographic boundaries required for the choropleth map in the frontend.
Output: Valid GeoJSON boundary asset and a validation script.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-mock-ingestion-geographic-mapping-validation/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create simplified Sleman kecamatan GeoJSON file</name>
  <files>frontend/public/geojson/sleman-kecamatan.geojson</files>
  <read_first>.planning/phases/02-mock-ingestion-geographic-mapping-validation/02-CONTEXT.md</read_first>
  <action>
    Create a 'frontend/public/geojson/' directory.
    Construct and save a valid, simplified GeoJSON file representing the 17 sub-districts of Sleman Regency, Yogyakarta.
    Ensure each feature contains a polygon under 'geometry' and properties including 'name' (e.g. 'Depok', 'Mlati', 'Ngemplak', 'Gamping', etc.).
  </action>
  <verify>node -e "JSON.parse(require('fs').readFileSync('frontend/public/geojson/sleman-kecamatan.geojson'))"</verify>
  <acceptance_criteria>
    - GeoJSON is successfully parsed by Node.js.
    - All 17 sub-districts of Sleman are defined as features.
  </acceptance_criteria>
  <done>Sleman kecamatan GeoJSON file is created and placed under public assets.</done>
</task>

<task type="auto">
  <name>Task 2: Build and run GeoJSON validation script</name>
  <files>scripts/validate-geojson.ts</files>
  <read_first>frontend/public/geojson/sleman-kecamatan.geojson</read_first>
  <action>
    Create a TypeScript validation script 'scripts/validate-geojson.ts' to verify that the GeoJSON file:
      1. Is a valid GeoJSON FeatureCollection.
      2. Contains exactly the 17 Sleman sub-districts.
      3. Geometry coordinates are within valid Sleman geographic coordinates (approx Longitude 110.2 to 110.5, Latitude -7.8 to -7.5).
    Run the script to validate.
  </action>
  <verify>npx tsx scripts/validate-geojson.ts</verify>
  <acceptance_criteria>
    - Script compiles and runs successfully.
    - Validation checks pass and print positive confirmation.
  </acceptance_criteria>
  <done>Validation script created and GeoJSON file verified.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Run `npx tsx scripts/validate-geojson.ts` and confirm output.
</verification>

<success_criteria>
- Sleman GeoJSON file exists and has correct format.
- Validation script reports zero issues.
</success_criteria>

<output>
After completion, create `.planning/phases/02-mock-ingestion-geographic-mapping-validation/02-01-SUMMARY.md`
</output>
