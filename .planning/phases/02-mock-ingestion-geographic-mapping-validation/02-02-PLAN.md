---
phase: 02-mock-ingestion-geographic-mapping-validation
plan: "02"
type: execute
wave: 1
depends_on: ["02-01"]
files_modified:
  - backend/package.json
  - backend/src/seed.ts
autonomous: true
requirements:
  - SEED-01
user_setup: []

must_haves:
  truths:
    - "Running 'npm run seed' in the backend directory completes successfully."
    - "At least 5,000 clinical records are successfully inserted into the RekamMedis table."
    - "The seeded records contain valid visit dates within the last 12 months, valid ICD-10 codes, and sub-district names that match the 17 Sleman kecamatan."
  artifacts:
    - "backend/src/seed.ts"
---

<objective>
Implement a Faker.js-powered CLI database seeder script in the Express.js backend to generate and ingest at least 5,000 realistic clinical records matching Sleman sub-districts.

Purpose: To populate the database with realistic historical and spatial data so that frontend charts, heatmaps, and anomalies can be verified.
Output: A seed script configured and run via npm command.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-mock-ingestion-geographic-mapping-validation/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @faker-js/faker in backend</name>
  <files>backend/package.json</files>
  <read_first>backend/package.json</read_first>
  <action>
    Install '@faker-js/faker' as a devDependency in the 'backend/' directory (version 10.4.0 as per GEMINI.md).
  </action>
  <verify>cd backend && npm list @faker-js/faker</verify>
  <acceptance_criteria>
    - @faker-js/faker is listed under devDependencies in backend/package.json.
  </acceptance_criteria>
  <done>Faker.js dependency is installed in the backend.</done>
</task>

<task type="auto">
  <name>Task 2: Implement and run the database seed script</name>
  <files>backend/src/seed.ts, backend/package.json</files>
  <read_first>backend/src/models/RekamMedis.ts</read_first>
  <action>
    Create 'backend/src/seed.ts' which initializes connection to PostgreSQL database, syncs the tables, and generates 5,000+ medical records.
    Use @faker-js/faker to mock:
      - id: UUID
      - tanggal_kunjungan: random dates spanning the last 12 months.
      - kode_icd10 & nama_penyakit: weighted random selection of common diseases (e.g. Ispa: J06.9, DBD: A90, Diare: A09, Flu: J11, Darah Tinggi: I10).
      - kecamatan_domisili: random selection from the 17 Sleman sub-districts (Depok, Mlati, Ngemplak, Gamping, etc.).
    Add a "seed": "tsx src/seed.ts" script to 'backend/package.json'.
    Run the seeder using 'npm run seed'.
  </action>
  <verify>cd backend && npm run seed</verify>
  <acceptance_criteria>
    - Seeder script completes with no errors.
    - Seeder logs output confirming successful insertion of at least 5,000 records.
  </acceptance_criteria>
  <done>Database seeder script implemented, registered, and executed successfully.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Verify count of records in 'RekamMedis' table is >= 5000.
</verification>

<success_criteria>
- The RekamMedis table contains at least 5,000 records.
- All record fields (tanggal_kunjungan, kode_icd10, nama_penyakit, kecamatan_domisili) are populated correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/02-mock-ingestion-geographic-mapping-validation/02-02-SUMMARY.md`
</output>
