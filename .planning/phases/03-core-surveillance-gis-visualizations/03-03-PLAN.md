---
phase: 03-core-surveillance-gis-visualizations
plan: "03"
type: execute
wave: 2
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - frontend/src/app/page.tsx
  - frontend/src/components/RegionDetailPanel.tsx
  - frontend/src/components/TrendsChart.tsx
autonomous: true
requirements:
  - MAP-02
  - MAP-03
user_setup: []

must_haves:
  truths:
    - "RegionDetailPanel displays sub-district caseload, population, and incidence rate."
    - "Incidence rate formula: (cases / population) * 10000 is correctly calculated."
    - "TrendsChart renders Recharts Line Chart with date range and disease filters."
    - "Pill badges filter diseases by toggling lines on/off with hover combined tooltips."
  artifacts:
    - "frontend/src/components/RegionDetailPanel.tsx"
    - "frontend/src/components/TrendsChart.tsx"
---

# Phase 3 - Plan 03: Region Detail Panel & Trends Recharts Chart

<objective>
Develop the dynamic Region Detail Panel and historical time-series Recharts chart on the Next.js frontend, complete with date filters, disease selection pill badges, and combined tooltips.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-surveillance-gis-visualizations/03-CONTEXT.md
@.planning/phases/03-core-surveillance-gis-visualizations/03-RESEARCH.md
@.planning/phases/03-core-surveillance-gis-visualizations/03-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Develop RegionDetailPanel component with API fetch</name>
  <files>frontend/src/components/RegionDetailPanel.tsx</files>
  <read_first>.planning/phases/03-core-surveillance-gis-visualizations/03-CONTEXT.md</read_first>
  <action>
    Create a new component frontend/src/components/RegionDetailPanel.tsx.
    The component receives the selected kecamatan name as a prop.
    Trigger fetching to GET /api/cases/region/:name.
    Calculate the normalized incidence rate per 10,000 residents using formula:
      `incidence_rate = (cases / population) * 10000`
    Display: Nama Kecamatan, Jumlah Kasus, Populasi Wilayah, and Angka Insidensi.
    Show a transparent loading spinner state while fetching.
  </action>
  <verify>cd frontend && npx tsc --noEmit</verify>
  <acceptance_criteria>
    - RegionDetailPanel compiles correctly.
    - Incidence rate calculation correctly rounds to two decimal places.
  </acceptance_criteria>
  <done>RegionDetailPanel component implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Develop Recharts TrendsChart component</name>
  <files>frontend/src/components/TrendsChart.tsx</files>
  <read_first>frontend/package.json</read_first>
  <action>
    Create a new component frontend/src/components/TrendsChart.tsx.
    Use Recharts ResponsiveContainer, LineChart, Line, XAxis, YAxis, Tooltip, Legend, and CartesianGrid.
    Implement a Combined Tooltip displaying all active disease counts for the hovered date.
    Ensure line colors respect the standard stack and UI-SPEC.
  </action>
  <verify>cd frontend && npx tsc --noEmit</verify>
  <acceptance_criteria>
    - TrendsChart compiles correctly.
    - Recharts chart scales nicely and displays line paths based on provided data.
  </acceptance_criteria>
  <done>TrendsChart component implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Develop disease filter pill badges and date filters</name>
  <files>frontend/src/app/page.tsx</files>
  <read_first>frontend/src/app/page.tsx</read_first>
  <action>
    Open page.tsx. Implement multi-select disease filter pill badges (toggle active/inactive) at the top-right of the TrendsChart.
    Implement date range dropdown filters (e.g. 30 days, 90 days, 1 year).
    Fetch GET /api/cases/temporal matching the active filters.
    Pass the aggregated data and active lines state to the TrendsChart.
  </action>
  <verify>cd frontend && npx tsc --noEmit</verify>
  <acceptance_criteria>
    - page.tsx compiles correctly.
    - Clicking pill badges correctly toggles the visibility of the corresponding disease lines on the chart.
  </acceptance_criteria>
  <done>Interactive filters implemented and integrated.</done>
</task>

<task type="auto">
  <name>Task 4: Integrate Detail Panel and TrendsChart in Main Dashboard</name>
  <files>frontend/src/app/page.tsx</files>
  <read_first>frontend/src/app/page.tsx</read_first>
  <action>
    Integrate the RegionDetailPanel and TrendsChart components into the main layout grid of the dashboard page.
    Coordinate state such that selecting a region on MapComponent updates RegionDetailPanel name prop.
    Ensure styling adheres to the Josefin Sans / Montserrat typography, glassmorphism card surfaces, and color themes.
  </action>
  <verify>cd frontend && npx tsc --noEmit</verify>
  <acceptance_criteria>
    - page.tsx compiles successfully.
    - Layout matches the visual mockups and design token definitions.
  </acceptance_criteria>
  <done>Main dashboard page fully integrated and styled.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Frontend compiles typecheck: `cd frontend && npx tsc --noEmit`.
- [ ] Dev server starts and runs the dashboard without console errors.
</verification>

<success_criteria>
- Clicking subdistricts updates Region Detail panel dynamically.
- Disease filters toggle Recharts lines successfully.
- Tooltip displays data for all selected lines on hover.
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-surveillance-gis-visualizations/03-03-SUMMARY.md`
</output>

## Artifacts this phase produces

### Files
- `frontend/src/components/RegionDetailPanel.tsx`
- `frontend/src/components/TrendsChart.tsx`

### Symbols
- Components: `RegionDetailPanel` (displays statistics on region select), `TrendsChart` (comparison Recharts line chart)
- Component State: active diseases, date range filters, selected kecamatan state in `page.tsx`

