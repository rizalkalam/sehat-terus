---
phase: 03-core-surveillance-gis-visualizations
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/package.json
  - backend/src/config/kecamatan.ts
  - backend/src/app.ts
  - backend/src/routes/cases.ts
  - backend/src/controllers/cases.ts
autonomous: true
requirements:
  - API-03
user_setup: []

must_haves:
  truths:
    - "Express.js API exposes GET /api/cases/spatial returning caseload grouped by kecamatan_domisili."
    - "Express.js API exposes GET /api/cases/temporal returning cases grouped by date and disease."
    - "Express.js API exposes GET /api/cases/region/:name returning case count and population for the sub-district."
  artifacts:
    - "backend/src/config/kecamatan.ts"
    - "backend/src/routes/cases.ts"
    - "backend/src/controllers/cases.ts"
---

# Phase 3 - Plan 01: Backend Spatial & Temporal API Endpoints

<objective>
Implement database-level aggregation endpoints in the Express.js backend to provide performant spatial-temporal data for the map and chart dashboards.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-surveillance-gis-visualizations/03-CONTEXT.md
@.planning/phases/03-core-surveillance-gis-visualizations/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Sleman kecamatan population data config</name>
  <files>backend/src/config/kecamatan.ts</files>
  <read_first>.planning/phases/03-core-surveillance-gis-visualizations/03-CONTEXT.md</read_first>
  <action>
    Create a configuration file backend/src/config/kecamatan.ts storing the population data for all 17 sub-districts of Sleman.
    Map: Turi (38000), Pakem (37000), Cangkringan (31000), Tempel (54000), Sleman (69000), Ngaglik (104000), Ngemplak (64000), Minggir (32000), Seyegan (50000), Mlati (102000), Moyudan (33000), Godean (71000), Gamping (108000), Depok (131000), Kalasan (83000), Berbah (56000), Prambanan (53000).
  </action>
  <verify>npx tsx -e "import { KECAMATAN_POPULATIONS } from './backend/src/config/kecamatan'; console.log(KECAMATAN_POPULATIONS.Depok)"</verify>
  <acceptance_criteria>
    - file backend/src/config/kecamatan.ts compiles correctly.
    - KECAMATAN_POPULATIONS exports all 17 Sleman sub-districts with realistic numbers.
  </acceptance_criteria>
  <done>Kecamatan population config file created.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Spatial Case Aggregation endpoint</name>
  <files>backend/src/controllers/cases.ts</files>
  <read_first>backend/src/models/RekamMedis.ts</read_first>
  <action>
    Implement case controller and a GET /api/cases/spatial endpoint.
    Use Sequelize grouping to count all RekamMedis records grouped by kecamatan_domisili.
    Support optional query params: start_date, end_date, and diseases (comma-separated ICD-10 codes).
    Response should contain: kecamatan name, total cases, and population (from config).
  </action>
  <verify>cd backend && npx tsc --noEmit</verify>
  <acceptance_criteria>
    - Controller code compiles cleanly.
    - Sequelize database query groups efficiently by kecamatan_domisili.
  </acceptance_criteria>
  <done>Spatial aggregation controller created.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Temporal Case Aggregation endpoint with dynamic interval</name>
  <files>backend/src/controllers/cases.ts</files>
  <read_first>backend/src/controllers/cases.ts</read_first>
  <action>
    Add a GET /api/cases/temporal endpoint to aggregate records by date and disease.
    Support start_date and end_date query parameters.
    Dynamically resolve aggregation interval:
      - Rentang &lt; 30 hari = harian (DATE_TRUNC 'day')
      - Rentang 31 - 180 hari = mingguan (DATE_TRUNC 'week')
      - Rentang &gt; 180 hari = bulanan (DATE_TRUNC 'month')
    Query parameter diseases (comma separated ICD-10 codes) filters the records.
    Return an array of objects containing visit_date, disease name/code, and case count.
  </action>
  <verify>cd backend && npx tsc --noEmit</verify>
  <acceptance_criteria>
    - Controller code compiles cleanly.
    - DATE_TRUNC dynamically changes depending on the length of start_date to end_date.
  </acceptance_criteria>
  <done>Temporal aggregation controller created.</done>
</task>

<task type="auto">
  <name>Task 4: Implement Sub-district Detail endpoint</name>
  <files>backend/src/controllers/cases.ts</files>
  <read_first>backend/src/controllers/cases.ts</read_first>
  <action>
    Add GET /api/cases/region/:name endpoint.
    Query total case count for the specific kecamatan name (filter by optional start_date and end_date).
    Retrieve the population from config.
    Return JSON: { name, population, cases }.
  </action>
  <verify>cd backend && npx tsc --noEmit</verify>
  <acceptance_criteria>
    - Controller code compiles cleanly.
    - returns JSON object with sub-district details.
  </acceptance_criteria>
  <done>Sub-district detail controller created.</done>
</task>

<task type="auto">
  <name>Task 5: Route registration and server integration</name>
  <files>backend/src/app.ts</files>
  <read_first>backend/src/app.ts</read_first>
  <action>
    Create backend/src/routes/cases.ts defining routes for the endpoints.
    Import routes in backend/src/app.ts and mount under '/api/cases'.
    Ensure CORS is fully configured to allow frontend connections on port 3000.
  </action>
  <verify>cd backend && npx tsc --noEmit</verify>
  <acceptance_criteria>
    - App starts and compiles cleanly.
    - CORS middleware is active.
  </acceptance_criteria>
  <done>Routes mounted in Express application.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Backend runs typecheck cleanly: `cd backend && npx tsc --noEmit`.
- [ ] Test the GET /api/cases/spatial endpoint using curl or node script, verify valid response structure.
</verification>

<success_criteria>
- GET /api/cases/spatial, /api/cases/temporal, and /api/cases/region/:name return correct data structures.
- DB grouping logic utilizes existing indexes properly.
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-surveillance-gis-visualizations/03-01-SUMMARY.md`
</output>

## Artifacts this phase produces

### Files
- `backend/src/config/kecamatan.ts`
- `backend/src/routes/cases.ts`
- `backend/src/controllers/cases.ts`

### Symbols
- Config: `KECAMATAN_POPULATIONS` (population metadata dictionary)
- Routes: `/api/cases/spatial`, `/api/cases/temporal`, `/api/cases/region/:name`
- Controller Functions: `getSpatialCases`, `getTemporalCases`, `getRegionDetail`

