---
phase: 03-core-surveillance-gis-visualizations
plan: "02"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - frontend/package.json
  - frontend/src/app/globals.css
  - frontend/src/app/page.tsx
  - frontend/src/components/MapComponent.tsx
autonomous: true
requirements:
  - MAP-01
user_setup: []

must_haves:
  truths:
    - "frontend/package.json contains leaflet and react-leaflet dependencies."
    - "frontend/src/app/globals.css imports leaflet.css."
    - "MapComponent renders interactive choropleth map dynamically."
    - "Kecamatan polygons on the map are colored according to case density fixed thresholds."
  artifacts:
    - "frontend/src/components/MapComponent.tsx"
---

# Phase 3 - Plan 02: Next.js Choropleth Map Component

<objective>
Develop the interactive Map Component in the Next.js frontend using react-leaflet to render Yogyakarta/Sleman kecamatan boundaries, coloring each district per fixed case density thresholds.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-surveillance-gis-visualizations/03-CONTEXT.md
@.planning/phases/03-core-surveillance-gis-visualizations/03-RESEARCH.md
@.planning/phases/03-core-surveillance-gis-visualizations/03-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install mapping dependencies in Next.js frontend</name>
  <files>frontend/package.json</files>
  <read_first>frontend/package.json</read_first>
  <action>
    Install 'leaflet', 'react-leaflet', and '@types/leaflet' in the frontend folder.
    Use --legacy-peer-deps if needed to satisfy peer dependencies on React 19.
  </action>
  <verify>cd frontend && npm list leaflet react-leaflet</verify>
  <acceptance_criteria>
    - Dependencies are listed in package.json.
    - Installation completes successfully.
  </acceptance_criteria>
  <done>Leaflet dependencies installed.</done>
</task>

<task type="auto">
  <name>Task 2: Import Leaflet CSS styles globally</name>
  <files>frontend/src/app/globals.css</files>
  <read_first>frontend/src/app/globals.css</read_first>
  <action>
    Add the leaflet stylesheet import at the top of frontend/src/app/globals.css:
    `@import "leaflet/dist/leaflet.css";`
    This ensures Leaflet map tiles are positioned correctly and not visually broken.
  </action>
  <verify>grep -q "leaflet.css" frontend/src/app/globals.css</verify>
  <acceptance_criteria>
    - globals.css contains the leaflet.css import line.
  </acceptance_criteria>
  <done>Leaflet CSS imported globally.</done>
</task>

<task type="auto">
  <name>Task 3: Develop interactive MapComponent with React Leaflet</name>
  <files>frontend/src/components/MapComponent.tsx</files>
  <read_first>frontend/public/geojson/sleman-kecamatan.geojson</read_first>
  <action>
    Create MapComponent.tsx which loads sleman-kecamatan.geojson boundary file.
    Use MapContainer, TileLayer, and GeoJSON components.
    - TileLayer URL: CartoDB Positron ('https://{s}.basemaps.cartocdn.com/light_all/{z}/{x}/{y}{r}.png').
    - Center Sleman: latitude -7.69, longitude 110.36, zoom level 11.
    - GeoJSON styling: Fill color mapped per Fixed Thresholds from spatial data:
      - &lt; 50 cases: Emerald/Green (`#34d399`)
      - 50 - 150 cases: Amber/Yellow (`#fbbf24`)
      - &gt; 150 cases: Rose/Red (`#f43f5e`)
    - Attach hover styles and click listeners. Clicking a region should callback selected region details to parent state.
  </action>
  <verify>cd frontend && npx tsc --noEmit</verify>
  <acceptance_criteria>
    - MapComponent compiles successfully.
    - Polygons color classification uses fixed thresholds from UI-SPEC.
  </acceptance_criteria>
  <done>MapComponent created.</done>
</task>

<task type="auto">
  <name>Task 4: Dynamic import MapComponent in Main Dashboard</name>
  <files>frontend/src/app/page.tsx</files>
  <read_first>frontend/src/app/page.tsx</read_first>
  <action>
    Open page.tsx. Import MapComponent dynamically using Next.js `next/dynamic` with `{ ssr: false }` to avoid node-side execution crashes.
    Replace the old mock SVG map with the dynamic MapComponent.
    Set up state callback handlers to capture selected sub-district click events.
    Ensure z-index overlay fixes are applied to Sidebar so it sits on top of Leaflet layers.
  </action>
  <verify>cd frontend && npx tsc --noEmit</verify>
  <acceptance_criteria>
    - page.tsx compiles successfully.
    - No SSR window error occurs when loading page.
  </acceptance_criteria>
  <done>MapComponent integrated dynamically into page.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Frontend compiles typecheck: `cd frontend && npx tsc --noEmit`.
- [ ] Run next development server and verify the map renders in the browser.
</verification>

<success_criteria>
- Peta choropleth Sleman displays on the main page.
- Sidebar z-index behaves correctly overlaying Map tiles.
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-surveillance-gis-visualizations/03-02-SUMMARY.md`
</output>

## Artifacts this phase produces

### Files
- `frontend/src/components/MapComponent.tsx`

### Symbols
- Components: `MapComponent` (interactive react-leaflet choropleth map component)
- Styles: `@import "leaflet/dist/leaflet.css"` in `globals.css`
- Dependencies: `leaflet`, `react-leaflet`, `@types/leaflet` in `package.json`

