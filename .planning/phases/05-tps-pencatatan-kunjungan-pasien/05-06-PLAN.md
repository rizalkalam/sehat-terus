---
phase: 05-tps-pencatatan-kunjungan-pasien
plan: "06"
type: execute
wave: 5
depends_on: ["05-04", "05-05"]
files_modified:
  - scripts/test-tps.ts
  - package.json
autonomous: true
requirements:
  - TPS-06
user_setup: []

must_haves:
  truths:
    - "A test script scripts/test-tps.ts is created."
    - "The test script tests logins, auth-gated endpoints, lookups, CRUD visits, prescription transaction safety, and cases summary."
    - "Running npm run test:tps succeeds when the backend is running."
  artifacts:
    - "scripts/test-tps.ts"
---

# Phase 5 - Plan 06: End-to-End Verification Test Suite

<objective>
Build an automated verification script to validate all 10 new endpoints and ensure the business logic constraints (role limits, transactional safety, etc.) are working properly.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/TPS-API-SPEC.md
@.planning/phases/05-tps-pencatatan-kunjungan-pasien/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/test-tps.ts with auth login and reference lookup tests</name>
  <files>scripts/test-tps.ts</files>
  <read_first>scripts/test-endpoints.ts</read_first>
  <action>
    Create scripts/test-tps.ts.
    - Write a login helper that authenticates (POST /api/auth/login) using cookie jars or cookie header parsing (extracting `st_auth` cookie from `set-cookie`).
    - Test all reference endpoints:
      - GET `/api/tps/referensi/penyakit`
      - GET `/api/tps/referensi/wilayah`
      - GET `/api/tps/referensi/obat`
      - GET `/api/tps/referensi/formula`
    - Verify that unauthenticated requests to these endpoints are rejected with 401.
  </action>
  <verify>npx tsx scripts/test-tps.ts --help || true</verify>
  <acceptance_criteria>
    - test-tps.ts compiles and executes helper code correctly.
  </acceptance_criteria>
  <done>Authentication helper and reference lookup tests implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Implement visit CRUD and prescription transaction tests</name>
  <files>scripts/test-tps.ts</files>
  <read_first>scripts/test-tps.ts</read_first>
  <action>
    In scripts/test-tps.ts, implement:
      - CRUD visit tests:
        - POST `/api/tps/kunjungan` (creates a visit).
        - GET `/api/tps/kunjungan` (verify it lists the visit).
        - PUT `/api/tps/kunjungan/:id` (updating the fields).
        - Verification that using an invalid kecamatan name throws 400.
      - Prescription and Transaction tests:
        - Attempt to create prescription with a non-apoteker/non-admin user (verify 403).
        - Create a prescription with an apoteker user (verify 210/201 and check that stock is decremented in DB).
        - Attempt to edit/delete the visit (verify 409).
        - Attempt to create a duplicate prescription (verify 409).
        - Attempt to create a prescription exceeding stock (verify 400).
      - Summary validation:
        - GET `/api/cases/summary` (verify statistics update).
  </action>
  <verify>npx tsx -p "import('./scripts/test-tps')" || true</verify>
  <acceptance_criteria>
    - test-tps.ts compiles cleanly.
    - Test steps verify both success and error responses.
  </acceptance_criteria>
  <done>Visit CRUD and prescription tests implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Register test command in package.json</name>
  <files>package.json</files>
  <read_first>package.json</read_first>
  <action>
    Add a script in root package.json:
    `"test:tps": "tsx scripts/test-tps.ts"`
  </action>
  <verify>npm run test:tps -- --dry-run || true</verify>
  <acceptance_criteria>
    - package.json contains the `test:tps` command.
  </acceptance_criteria>
  <done>Command added to package.json.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- Run the full verification script: `npm run test:tps` (ensure backend is running).
</verification>

<success_criteria>
- The test suite runs and outputs a full green report for all 10 endpoints.
</success_criteria>

<output>
After completion, create `.planning/phases/05-tps-pencatatan-kunjungan-pasien/05-06-SUMMARY.md`
</output>
