---
phase: 05-tps-pencatatan-kunjungan-pasien
plan: "03"
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - backend/src/controllers/tps/kunjungan.ts
  - backend/src/routes/tps.ts
autonomous: true
requirements:
  - TPS-04
user_setup: []

must_haves:
  truths:
    - "POST /api/tps/kunjungan creates a RekamMedis record with authenticated user's faskes_id and dicatat_oleh."
    - "GET /api/tps/kunjungan retrieves visits for user faskes, including ada_resep indicator."
    - "GET /api/tps/kunjungan/:id retrieves single visit details with resep details."
    - "PUT /api/tps/kunjungan/:id updates visit only if no resep is attached."
    - "DELETE /api/tps/kunjungan/:id deletes visit only if no resep is attached."
  artifacts:
    - "backend/src/controllers/tps/kunjungan.ts"
    - "backend/src/routes/tps.ts"
---

# Phase 5 - Plan 03: Patient Visits CRUD Endpoints

<objective>
Implement CRUD operations for patient visits (RekamMedis) with validation of Sleman districts and auth constraints.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/TPS-API-SPEC.md
@.planning/phases/05-tps-pencatatan-kunjungan-pasien/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create kunjungan controller and implement createKunjungan</name>
  <files>backend/src/controllers/tps/kunjungan.ts</files>
  <read_first>backend/src/models/RekamMedis.ts</read_first>
  <action>
    Create backend/src/controllers/tps/kunjungan.ts.
    Implement `createKunjungan`:
      - Accept `tanggal_kunjungan`, `kode_icd10`, `nama_penyakit`, `kecamatan_domisili`.
      - Check if `kecamatan_domisili` matches a district in the `Wilayah` table (throw 400 error if not found).
      - Ensure `tanggal_kunjungan` is not in the future.
      - Inject `faskes_id` from `req.user.faskes_id` and `dicatat_oleh` from `req.user.id`.
      - Create and return 201 status with the new RekamMedis record.
  </action>
  <verify>cd backend && npx tsc --noEmit</verify>
  <acceptance_criteria>
    - kunjungan.ts compiles.
    - createKunjungan is implemented and exported.
  </acceptance_criteria>
  <done>createKunjungan endpoint handler created.</done>
</task>

<task type="auto">
  <name>Task 2: Implement listKunjungan and getKunjunganById</name>
  <files>backend/src/controllers/tps/kunjungan.ts</files>
  <read_first>backend/src/controllers/tps/kunjungan.ts</read_first>
  <action>
    In kunjungan.ts, implement:
      - `listKunjungan`: Read visits for the user's `faskes_id`. Support optional filter query params: `tanggal`, `kode_icd10`, `kecamatan`, `page`, `limit` (max 100). Calculate `ada_resep` as a boolean indicating if an associated Resep exists. Return data and meta.
      - `getKunjunganById`: Read details of a specific visit by ID, checking that it belongs to the user's `faskes_id`. Include the associated Resep and ResepItem (with details of Obat or FormulaRacikan).
  </action>
  <verify>cd backend && npx tsc --noEmit</verify>
  <acceptance_criteria>
    - kunjungan.ts compiles.
    - listKunjungan handles pagination and filters.
    - getKunjunganById returns associated resep items structure correctly.
  </acceptance_criteria>
  <done>listKunjungan and getKunjunganById handlers implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Implement updateKunjungan and deleteKunjungan</name>
  <files>backend/src/controllers/tps/kunjungan.ts</files>
  <read_first>backend/src/controllers/tps/kunjungan.ts</read_first>
  <action>
    In kunjungan.ts, implement:
      - `updateKunjungan`: Update a visit by ID. If there is an associated Resep in the database, return a 409 Conflict status. Otherwise, update requested fields (`kode_icd10`, `nama_penyakit`, `kecamatan_domisili`, `tanggal_kunjungan`) and return the updated object.
      - `deleteKunjungan`: Delete a visit by ID. Check for associated Resep (return 409 if exists). Otherwise, delete and return success message.
    Both must enforce that the visit belongs to the user's `faskes_id`.
  </action>
  <verify>cd backend && npx tsc --noEmit</verify>
  <acceptance_criteria>
    - kunjungan.ts compiles.
    - update and delete block modifications if a prescription (resep) is already attached.
  </acceptance_criteria>
  <done>updateKunjungan and deleteKunjungan handlers implemented.</done>
</task>

<task type="auto">
  <name>Task 4: Register kunjungan routes in routes/tps.ts</name>
  <files>backend/src/routes/tps.ts</files>
  <read_first>backend/src/routes/tps.ts</read_first>
  <action>
    Import the kunjungan handlers in routes/tps.ts.
    Define routes:
      - POST `/kunjungan` -> createKunjungan
      - GET `/kunjungan` -> listKunjungan
      - GET `/kunjungan/:id` -> getKunjunganById
      - PUT `/kunjungan/:id` -> updateKunjungan
      - DELETE `/kunjungan/:id` -> deleteKunjungan
    Ensure JSDoc Swagger documentation is added above each route according to the spec.
  </action>
  <verify>cd backend && npx tsc --noEmit</verify>
  <acceptance_criteria>
    - routes/tps.ts compiles cleanly.
    - Routes are mapped correctly and documented with OpenAPI JSDoc.
  </acceptance_criteria>
  <done>Routes for kunjungan registered and documented.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- Run backend server.
- Test the CRUD endpoints via curl/HTTP client:
  - Create a visit. Check response fields.
  - Read the list of visits (with default filter and explicit filters).
  - Update the created visit.
  - Delete a visit and verify it is removed from the database.
- Verify Swagger documentation displays these endpoints properly.
</verification>

<success_criteria>
- Kunjungan endpoints operate successfully and enforce faskes-level scoping.
- Business rules (blocking update/delete when resep exists) are correctly enforced with 409 status code.
</success_criteria>

<output>
After completion, create `.planning/phases/05-tps-pencatatan-kunjungan-pasien/05-03-SUMMARY.md`
</output>
