---
phase: 05-tps-pencatatan-kunjungan-pasien
plan: "02"
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - backend/src/controllers/tps/referensi.ts
  - backend/src/routes/tps.ts
  - backend/src/app.ts
autonomous: true
requirements:
  - TPS-03
user_setup: []

must_haves:
  truths:
    - "GET /api/tps/referensi/penyakit returns list of common ICD-10 codes and names."
    - "GET /api/tps/referensi/wilayah returns list of Sleman districts."
    - "GET /api/tps/referensi/obat returns available medicines with stock > 0 in user's faskes."
    - "GET /api/tps/referensi/formula returns formulas where all components are in stock."
  artifacts:
    - "backend/src/controllers/tps/referensi.ts"
    - "backend/src/routes/tps.ts"
---

# Phase 5 - Plan 02: Reference Endpoints (Dropdown Lookups)

<objective>
Implement the lookups / reference endpoints used to populate dropdowns in the TPS input form.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/TPS-API-SPEC.md
@.planning/phases/05-tps-pencatatan-kunjungan-pasien/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create referensi controller and implement listPenyakit & listWilayah</name>
  <files>backend/src/controllers/tps/referensi.ts</files>
  <read_first>backend/src/models/Wilayah.ts</read_first>
  <action>
    Create backend/src/controllers/tps/referensi.ts.
    Implement `listPenyakit` which returns a static list of common ICD-10 codes/diseases:
      - J06.9 (ISPA)
      - J11 (Influenza)
      - A09 (Diare)
      - A90 (DBD)
      - I10 (Darah Tinggi)
      - A27.9 (Leptospirosis)
      - B05 (Campak)
      - A36 (Difteri)
      - A80 (Polio)
      - J09 (Flu Burung)
    Implement `listWilayah` which queries the `Wilayah` table and returns ID, nama_kecamatan, and kabupaten.
  </action>
  <verify>cd backend && npx tsc --noEmit</verify>
  <acceptance_criteria>
    - referensi.ts compiles.
    - listPenyakit and listWilayah handlers are exported.
  </acceptance_criteria>
  <done>listPenyakit and listWilayah implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Implement listObat & listFormula in referensi controller</name>
  <files>backend/src/controllers/tps/referensi.ts</files>
  <read_first>backend/src/models/Stok.ts</read_first>
  <action>
    Add `listObat` and `listFormula` to referensi.ts.
    - `listObat`: Query `Obat` and `Stok` tables. Only return medicines where `stok.jumlah_tersedia > 0` in the user's faskes (`req.user.faskes_id`). Support search query `q` and group filter `golongan`.
    - `listFormula`: Query `FormulaRacikan`, `FormulaKomponen`, and `Stok` tables. Only return formulas where all components have stock in the user's faskes (`req.user.faskes_id` has stok of component with jumlah_tersedia > 0).
  </action>
  <verify>cd backend && npx tsc --noEmit</verify>
  <acceptance_criteria>
    - referensi.ts compiles cleanly.
    - listObat and listFormula filter by user faskes_id and stock correctly.
  </acceptance_criteria>
  <done>listObat and listFormula implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Create TPS routes and register in Express application</name>
  <files>backend/src/routes/tps.ts</files>
  <read_first>backend/src/routes/auth.ts</read_first>
  <action>
    Create backend/src/routes/tps.ts.
    Set up routes under `/referensi`:
      - GET `/referensi/penyakit` -> listPenyakit
      - GET `/referensi/wilayah` -> listWilayah
      - GET `/referensi/obat` -> listObat
      - GET `/referensi/formula` -> listFormula
    Apply `requireAuth` middleware to all these routes.
    Add OpenAPI JSDoc annotations for these 4 endpoints following the format in routes/auth.ts.
    In backend/src/app.ts: Import `tpsRouter` and mount under `/api/tps`.
  </action>
  <verify>cd backend && npx tsc --noEmit</verify>
  <acceptance_criteria>
    - backend routes are successfully compiled.
    - routes/tps.ts correctly imports and references the controllers.
  </acceptance_criteria>
  <done>TPS route file created and registered in app.ts.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- Run backend server in development mode.
- Test the lookup routes using curl/HTTP client:
  - GET `/api/tps/referensi/penyakit` (requires login cookie)
  - GET `/api/tps/referensi/wilayah`
  - GET `/api/tps/referensi/obat`
  - GET `/api/tps/referensi/formula`
- Check Swagger UI at `http://localhost:5000/api/docs` to ensure they are visible under the TPS / Referensi tags.
</verification>

<success_criteria>
- The 4 lookup endpoints return correct lists in correct structure.
- JSDoc is present and registered correctly in Swagger.
</success_criteria>

<output>
After completion, create `.planning/phases/05-tps-pencatatan-kunjungan-pasien/05-02-SUMMARY.md`
</output>
