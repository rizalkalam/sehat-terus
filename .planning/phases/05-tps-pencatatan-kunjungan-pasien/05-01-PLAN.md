---
phase: 05-tps-pencatatan-kunjungan-pasien
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/models/RekamMedis.ts
  - backend/src/models/index.ts
  - backend/src/seed.ts
  - backend/src/seedAll.ts
autonomous: true
requirements:
  - TPS-01
user_setup: []

must_haves:
  truths:
    - "Model RekamMedis has column dicatat_oleh (UUID) associated with Pengguna model."
    - "Model index.ts defines the hasMany/belongsTo associations between Pengguna and RekamMedis."
    - "seed.ts queries existing FasilitasKesehatan and Pengguna records to assign valid faskes_id and dicatat_oleh to seeded RekamMedis records."
    - "seedAll.ts seeds 10-20 mock visits for today and last week with valid faskes_id and dicatat_oleh."
  artifacts:
    - "backend/src/models/RekamMedis.ts"
    - "backend/src/models/index.ts"
    - "backend/src/seed.ts"
    - "backend/src/seedAll.ts"
---

# Phase 5 - Plan 01: Database Schema Update & Seeder Update

<objective>
Update the RekamMedis database schema by adding the dicatat_oleh column and association with the Pengguna model, then update both seed.ts and seedAll.ts to populate these columns correctly.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tps-pencatatan-kunjungan-pasien/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dicatat_oleh column to RekamMedis model</name>
  <files>backend/src/models/RekamMedis.ts</files>
  <read_first>backend/src/models/RekamMedis.ts</read_first>
  <action>
    Modify RekamMedis.ts to include the dicatat_oleh attribute (DataTypes.UUID, allowNull: true).
    Declare the field in the RekamMedis class: `declare dicatat_oleh: string | null;`.
  </action>
  <verify>cd backend && npx tsc --noEmit</verify>
  <acceptance_criteria>
    - RekamMedis class declares and initializes the dicatat_oleh property.
    - TypeScript compiles without errors.
  </acceptance_criteria>
  <done>RekamMedis model updated with dicatat_oleh column.</done>
</task>

<task type="auto">
  <name>Task 2: Define association in models/index.ts</name>
  <files>backend/src/models/index.ts</files>
  <read_first>backend/src/models/index.ts</read_first>
  <action>
    Add associations between Pengguna and RekamMedis in models/index.ts.
    Specifically:
    `Pengguna.hasMany(RekamMedis, { foreignKey: 'dicatat_oleh', as: 'rekam_medis_dicatat' });`
    `RekamMedis.belongsTo(Pengguna, { foreignKey: 'dicatat_oleh', as: 'pencatat' });`
  </action>
  <verify>cd backend && npx tsc --noEmit</verify>
  <acceptance_criteria>
    - Associations defined cleanly.
    - Typecheck compiles without errors.
  </acceptance_criteria>
  <done>Associations defined in models/index.ts.</done>
</task>

<task type="auto">
  <name>Task 3: Update seed.ts to assign valid faskes_id and dicatat_oleh</name>
  <files>backend/src/seed.ts</files>
  <read_first>backend/src/seed.ts</read_first>
  <action>
    Modify backend/src/seed.ts.
    Before generating the 5,500 records:
    1. Import FasilitasKesehatan and Pengguna from './models'.
    2. Query an existing faskes (e.g. 'Klinik Sehat Terus Sleman') and a user (e.g. 'apoteker@sehatterus.id').
    3. If none exist, skip or throw a descriptive error, but in normal run since seedAll.ts runs first, they will exist. As a fallback, handle cases where they might be empty.
    4. Set `faskes_id` and `dicatat_oleh` properties on each generated record using the retrieved IDs.
  </action>
  <verify>cd backend && npx tsc --noEmit</verify>
  <acceptance_criteria>
    - seed.ts compiles.
    - Generated mock records contain the queried faskes_id and dicatat_oleh.
  </acceptance_criteria>
  <done>seed.ts seeder updated.</done>
</task>

<task type="auto">
  <name>Task 4: Update seedAll.ts to seed example visits and run database sync</name>
  <files>backend/src/seedAll.ts</files>
  <read_first>backend/src/seedAll.ts</read_first>
  <action>
    Modify backend/src/seedAll.ts.
    1. Import RekamMedis model (it is already in './models' exports, just import it).
    2. After seeding users and faskes, add a step to seed 10-20 visits (RekamMedis records) for today and the past 7 days.
    3. Ensure they use valid `faskes_id` (from the seeded faskes) and `dicatat_oleh` (from the seeded apoteker/manajer).
    4. Ensure they have varied ICD-10 codes, kecamatan_domisili (valid Sleman kecamatan), and dates.
  </action>
  <verify>cd backend && npx tsc --noEmit</verify>
  <acceptance_criteria>
    - seedAll.ts compiles.
    - Database is successfully updated with the new schema (alter: true) and seeded with mock visits when run.
  </acceptance_criteria>
  <done>seedAll.ts seeder updated.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- Run the schema update sync and seeding: `npm run seed:all` inside backend or root directory.
- Verify the table schema in PostgreSQL (column `dicatat_oleh` exists in `"RekamMedis"`).
- Run `npm run seed` to ensure the 5,500 records are seeded correctly.
</verification>

<success_criteria>
- `"RekamMedis"` table in database contains `dicatat_oleh` column.
- Seeding commands run without errors and successfully populate the new fields.
</success_criteria>

<output>
After completion, create `.planning/phases/05-tps-pencatatan-kunjungan-pasien/05-01-SUMMARY.md`
</output>
